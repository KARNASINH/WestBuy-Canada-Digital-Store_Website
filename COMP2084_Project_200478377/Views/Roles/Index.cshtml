@model IEnumerable<COMP2084_Project_200478377.Models.Role>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RoleName">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RoleName">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RoleName">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
